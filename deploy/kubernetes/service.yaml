apiVersion: v1
kind: Service
metadata:
  name: sona-ai-os
  namespace: sona-ai-os
  labels:
    app: sona-ai-os
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: sona-ai-os
