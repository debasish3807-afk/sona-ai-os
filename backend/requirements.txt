# Sona AI OS - Backend Dependencies
# ==================================
# Python 3.12+

# --- Web Framework ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# --- Data Validation & Settings ---
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# --- Structured Logging ---
structlog>=24.4.0,<25.0.0

# --- HTTP Client (async) ---
httpx>=0.27.0,<1.0.0

# --- Configuration ---
python-dotenv>=1.0.0,<2.0.0

# --- Security ---
python-jose[cryptography]>=3.3.0,<4.0.0
argon2-cffi>=23.1.0,<26.0.0

# --- Database (install for production PostgreSQL) ---
asyncpg>=0.29.0,<1.0.0

# --- Cache (install for production Redis) ---
redis[hiredis]>=5.0.0,<6.0.0

# --- Vector Database (Qdrant uses httpx, no extra dep needed) ---
# Qdrant client uses httpx REST API — no additional package required.

# --- AI Providers (optional, for cloud provider integration) ---
# openai>=1.30.0
# anthropic>=0.25.0
